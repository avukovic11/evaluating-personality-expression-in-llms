# Pinned with lower bounds for cross-platform compatibility (Windows / macOS / Linux).
# For GPU support, install a CUDA-matched torch wheel BEFORE running `pip install -r requirements.txt`,
# e.g. on Linux/Windows with CUDA 12.1:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# On macOS, the default CPU/MPS wheel is fine.

# --- Core ML ---
torch>=2.1,<3.0
transformers>=4.40
tokenizers>=0.19
accelerate>=0.30
datasets>=2.18
sentencepiece>=0.2.0          # required by DeBERTa-v3 tokenizer
protobuf>=4.0                 # required by tokenizers v5 to parse SentencePiece .model files

# --- Classical ML / stats ---
scikit-learn>=1.3
iterative-stratification>=0.1.7
numpy>=1.24
pandas>=2.0
scipy>=1.10

# --- Interpretability ---
shap>=0.45

# --- OpenAI generation ---
openai>=1.30
python-dotenv>=1.0

# --- NLP feature extraction (for the LIWC-style baseline) ---
nltk>=3.8
nrclex>=4.0

# --- Plotting ---
matplotlib>=3.7
seaborn>=0.13

# --- Utilities ---
tqdm>=4.66

# Jupyter / notebook deps live in requirements-dev.txt — install separately if you
# want to run the notebooks locally. On Colab the Jupyter stack is preinstalled.
